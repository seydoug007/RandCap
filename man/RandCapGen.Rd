% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RandCap.R
\name{RandCapGen}
\alias{RandCapGen}
\title{Generate Randomized Allocation List with Blocking and Stratification}
\usage{
RandCapGen(
  n,
  id_prefix = NULL,
  block_prefix = NULL,
  block_sizes,
  arms,
  ratio = NULL,
  strat_vars = NULL,
  strat_vars_prefix = NULL,
  seed = NULL,
  uneq_beg = FALSE,
  uneq_mid = FALSE,
  uneq_min = 1,
  uneq_maxit = 1000,
  project_acronym = NULL
)
}
\arguments{
\item{n}{Integer. The total number of participants to be randomized.}

\item{id_prefix}{Character string. A prefix for participant IDs in the randomization list.}

\item{block_prefix}{Character string. A prefix for each block in the randomization list.}

\item{block_sizes}{Integer vector. Specifies the sizes of each block for blocked randomization.}

\item{arms}{Character vector. The names of treatment arms/groups.}

\item{ratio}{Integer vector. The allocation ratio for each treatment arm, e.g., \code{c(1, 2)} for a 1:2 ratio.}

\item{strat_vars}{Character vector. Names of stratification variables to ensure balanced randomization across subgroups.}

\item{strat_vars_prefix}{Character string. Prefix to add to stratification variable names in the output.}

\item{seed}{Integer. Seed for random number generation to ensure reproducibility.}

\item{uneq_beg}{Integer. The beginning of unequal allocation in blocks, used when blocks differ in allocation ratios.}

\item{uneq_mid}{Integer. The midpoint for unequal allocation within blocks.}

\item{uneq_min}{Integer. Minimum size of blocks when using unequal allocation.}

\item{uneq_maxit}{Integer. Maximum iterations to achieve the target allocation ratio.}

\item{project_acronym}{Character string. Acronym for the project, used in file names for easy identification.}
}
\value{
A list containing two data frames:
\describe{
\item{randomization_list}{A complete randomization table with participant IDs, blocks, treatment arms, and stratification variables.}
\item{redcap_import}{A simplified randomization list ready for import into REDCap.}
}
}
\description{
\code{RandCapGen} generates a randomization list that supports blocked and stratified randomization with customizable allocation ratios.
This function is ideal for designing randomized allocation schemes in clinical trials or research studies that require balanced participant allocation.
}
\examples{
# Basic example with 100 participants, two treatment arms, and a 1:1 allocation ratio
randomization_object<-RandCapGen(
  n = 100,
  id_prefix = "ID_",
  block_prefix = "Block_",
  arms = c("A", "B"),
  block_sizes = c(2, 4),
  ratio = c(1, 1),# optionnal when equal allocation
  strat_vars = list(sex=c("Male","Female"),age=c("Young","Old")),
  seed = 1234,
)
}
\seealso{
\code{\link{RandCapBalance}} for assessing randomization balance,
\code{\link{RandCapSettings}} for exporting randomization settings,
\code{\link{RandCapTable}},
and \code{\link{RandCapProd}} for exporting randomization tables for REDCap.
}
